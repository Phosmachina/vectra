extends shared/layout

block header

block content
    h1 Login
    form(ajax-func='login')
        +inputEmail
        +inputRequestPassword
        input(type='submit' value="Login")

block footer

